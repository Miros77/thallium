package thallium.fabric.chunk;

import it.unimi.dsi.fastutil.Hash;
import it.unimi.dsi.fastutil.longs.Long2ObjectOpenHashMap;

/**
 * Removed because Sodium thinks they hold copyright for every fastutil implementation of chunkmanagers
 * TODO report to fastutil
 */
public class ThalliumClientChunkManager extends ClientChunkManager {

    public ThalliumClientChunkManager(ClientWorld world, int loadDistance) {
        super(world, loadDistance);
        // TODO Auto-generated constructor stub
    }
}